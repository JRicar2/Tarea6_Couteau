import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jricar2.tarea6',
  appName: 'Couteau',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
